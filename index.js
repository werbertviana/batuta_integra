/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Navegacao from './src/screens/Login/Login';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Navegacao);
